「找出一條最長的連續子陣列，該子陣列中最多只能包含兩種不同的數字。」
一、 題目深入解析
想像你走在一排樹前面，手裡只有兩個籃子：
每棵樹只產一種水果（用數字代表種類）。
每個籃子只能裝「同一種」水果。這代表你最多只能收集 2 種 水果。
你必須從某棵樹開始，連續地往後摘，不能跳過任何一棵樹。
當你遇到「第 3 種」水果，且你的兩個籃子都已經裝了不同種類的水果時，你就必須停止。
你的目標是：摘到最多顆的水果（子陣列的最大長度）。
二、 解題思路：滑動窗口
這題的思路跟第 209 題非常像，但方向相反。209 題是找「符合條件的最短」，這題是找「符合條件的最長」。
右指標（擴張）：
右指標 right 不斷向右移動，把遇到的水果「丟進籃子」。
我們需要一個工具（資料結構）來記錄目前籃子裡有哪些水果，以及每一種水果分別有幾顆。
判斷條件：
每加入一顆新水果，就檢查：「現在籃子裡水果的種類是否超過 2 種？」
左指標（收縮）：
如果籃子裡的種類超過 2 種，代表我們必須拋棄掉一些左邊的水果，直到種類回到 2 種為止。
左指標 left 開始向右移動：
從記錄中減去 fruits[left] 的數量。
關鍵點：如果某種水果的數量減到 0，必須把這種水果從籃子的記錄中徹底移除，這樣種類數量才會真的減少。
更新答案：
在每一輪（當種類 ≤2 時），計算當前窗口的長度 right - left + 1，並記錄最大值。
三、 實作技巧
為了順利寫出程式碼，你需要掌握以下技巧：
1. 使用雜湊表 (Hash Map / Dictionary)
你需要記錄「水果種類」與「出現次數」的對應關係。
在 C++ 中可以使用 unordered_map<int, int>。
關鍵操作：當 map[fruits[left]] 減到 0 時，要執行 map.erase(fruits[left])。如果只把數值變 0 但不移除 key，map.size() 依然會是 3，導致邏輯錯誤。
2. 窗口更新的時機
這題是找「最大」長度，通常在右指標移動、且左指標收縮完成（確保窗口合法）後，才去更新 max_len。
3. 與 209 題的對比
209 題 (Shortest)：當條件滿足時，縮小窗口來找「更短」的。
904 題 (Longest)：當條件「不」滿足時，縮小窗口直到「重新滿足」條件，然後在滿足條件的過程中找「更長」的。
4. 複雜度提醒
時間複雜度：雖然有 while 嵌套在 for 裡面，但每個元素同樣只被進出窗口一次，所以是 O(n)。
空間複雜度：因為最多只會存 3 種水果的資訊，所以空間複雜度其實是 O(1)（常數空間）。
四、 思考練習
在動手寫之前，你可以先想一下：
如果題目改成「三個籃子」（最多 3 種水果），你的程式碼需要改動哪裡？
如果不用 unordered_map，還有什麼方式可以記錄水果種類？（提示：如果水果種類的數值範圍很小，可以用陣列代替）。

'''
class Solution {
public:
    int totalFruit(vector<int>& fruits) {
        int fastIndex=0, slowIndex=0;
        int max_fruit=0;
        unordered_map<int, int> basket;

        for(fastIndex=0; fastIndex<fruits.size(); fastIndex++){
            basket[fruits[fastIndex]]++;

            while(basket.size()>2){
                basket[fruits[slowIndex]]--;

                if(basket[fruits[slowIndex]]==0) 
                    basket.erase(fruits[slowIndex]);

                slowIndex++;
            }

            max_fruit=max(max_fruit, fastIndex-slowIndex+1);
        }

        return max_fruit;
    }
};
'''
