/*
一、 題目講解
情境：給你一個非負整數的陣列 nums，你一開始站在陣列的第一個位置（索引 0）。
規則：陣列中的每一個數字 nums[i]，代表你在該位置**「最多」**可以往前跳躍的格數。
注意是「最多」，代表你可以選擇跳 1 格、2 格，一直到 nums[i] 格。
目標：判斷你是否能成功跳到陣列的最後一個位置。能的話回傳 true，否則回傳 false。
情境比喻：
你可以把這題想像成**「手電筒的照明範圍」或是「車子加油」**。你每走到一個新地點，就可以獲得新的照明距離（或油量）。問題是：你的光照不照得到終點？如果中間遇到沒有油（遇到 0）且靠慣性也滑不過去的死胡同，那就到不了終點。
二、 解題思路：貪婪演算法 (Greedy)
這題用動態規劃（DP）會達到 O(n^2) 的時間複雜度，面試官要看的是你能不能想到 O(n) 的貪婪解法。貪婪法有兩種非常直覺的視角：

思路一：由前往後掃描（維護「最遠可達範圍」） ★ 最主流
我們不關心「每一步具體要跳到哪裡」，我們只關心**「我目前所能觸及的極限位置在哪」**。
設定一個變數 maxReach = 0，代表目前為止，我們能跳到的最遠索引。
從頭開始遍歷陣列中的每一個位置 i：
致命檢查：如果當前的位置 i 已經大於 maxReach，代表我連站在這裡的資格都沒有（中間斷檔了），直接宣告失敗，回傳 false。
更新極限：如果我能走到 i，那站在這裡，我最遠可以跳到 i + nums[i]。拿這個新極限去更新 maxReach（取歷史最大值）。
提早慶祝：如果更新後的 maxReach 已經大於或等於「最後一個位置的索引」，代表一定能到終點，直接回傳 true。

思路二：由後往前推（目標左移法） ★ 面試官會覺得你很酷
這是一個逆向思維：既然我們要到終點，不如我們從終點往回看，看誰能一步跳到終點？
設定一個變數 target = 陣列最後一個索引。
從倒數第二個位置往回走（i 從 n-2 到 0）：
如果站在 i 的位置，能跳到的最遠距離 i + nums[i] ≥ target，這代表只要我能到 i，我就一定能到終點。
既然如此，我們就把「終點」的標準放低，把 target 更新為 i（目標左移）。
遍歷結束後，只要檢查最後 target 有沒有成功被推回到起點 0 即可。
*/

class Solution {
public:
    bool canJump(vector<int>& nums) {
        int maxReach=0;

        for(int i=0; i<nums.size();i++){
            if(i>maxReach) return false;
            
            if((i+nums[i])>maxReach) maxReach=i+nums[i];

            if(maxReach>=nums.size()) return true;
        }

        return true;
    }
};
