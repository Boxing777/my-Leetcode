/*
一、 題目白話講解 
什麼是 H-Index？ 假設你是一位學者，你發表了 N N 篇論文。H-Index 是一個用來衡量你學術影響力的指標。 
它的定義是：「你最多有 h 篇論文，且這 h 篇論文『每一篇』都『至少』被引用了 h 次。」 
舉例：你的論文引用數是 [3, 0, 6, 1, 5] (總共 5 篇)。 
你有沒有 1 篇文章引用 ≥ 1 次？ 你有沒有 2 篇文章引用 ≥ 2 次？你有沒有 3 篇文章引用 ≥ 3 次？有 (3, 6, 5 都大於等於 3)。 
你有沒有 4 篇文章引用 ≥ 4 次？沒有 (只有 6, 5 兩篇大於 4)。 所以你的 H-Index 就是 3。 

二、 解題思路 
面試時，建議你向面試官展示以下兩個層次的思考過程： 

層次一：直覺的「排序法」 (Sorting) —— 時間 O(nlogn) 
當題目提到「大於等於某個數量的個數」時，排序永遠是第一個該想到的武器。 
由大到小排序：把引用數最高的文章排在前面。例如排成 [6, 5, 3, 1, 0]。 
遍歷並配對： 
第 1 篇論文，引用數 6 → 6 ≥ 2 (成立，代表至少有 1 篇引用 ≥ 1) 
第 2 篇論文，引用數 5 → 5 ≥ 2 (成立，代表至少有 2 篇引用 ≥ 2) 
第 3 篇論文，引用數 3 → 3 ≥ 3 (成立，代表至少有 3 篇引用 ≥ 3) 
第 4 篇論文，引用數 1 → 1 ≱ 4 (失敗！) 
結論：當發現某篇文章的引用數小於它目前的排名時，前一個排名就是最大的 H-Index。 

層次二：面試官最愛的「計數排序 / 桶排序」 (Bucket Sort) —— 時間 O(n) 排序法很好，但面試官會問：「能不能不要排序，在 O(n) 時間內解出來？」
這時就要利用這題的隱藏邊界特性。 尋找上限：如果你總共只有 N 篇論文，你的 H-Index 最大也只能是 N （就算你有一篇論文被引用一萬次，它也只是一篇論文）。 
準備桶子 (Buckets)：建立一個長度為 N+1 的陣列（索引 0 到 N ），用來統計「每個引用數」分別有幾篇論文。 
分類統計： 遍歷原始引用數陣列。 
如果一篇文章的引用數大於等於 N ，就統統丟進第 N 個桶子裡（因為超過 N 的引用數對提升 H-Index 沒有額外幫助，當作 N 來看待即可）。
否則，就丟進對應的桶子裡。 從後往前累加： 從最高的桶子（索引 N ）開始往回看，設定一個計數器 total_papers 記錄「目前看過的總文章數」。 
每次把當前桶子的文章數加進 total_papers。 核心判斷：一旦發現 total_papers 大於或等於「當前桶子的索引（引用數）」時，這個索引就是我们要找的最大 H-Index！
*/

class Solution {
public:
    int hIndex(vector<int>& citations) {
        int n=citations.size();
        vector<int> cit(n+1);

        for(int i=0; i<citations.size(); i++){
            if(citations[i]>citations.size()){
                cit[citations.size()]++;
            }else{
                cit[citations[i]]++;
            }
        }

        int ans=0, count=0;
        for(int i=cit.size()-1; i>=0; i--){
            count+=cit[i];
            if(count>=i){
                ans=i; 
                break;
            }
        }

        return ans;
    }
};
