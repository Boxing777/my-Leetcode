/*
一、 題目重點 目標：將陣列向右旋轉 k k 步。 右旋轉的意思：最後面的元素會跑到最前面。 
例如：[1, 2, 3, 4, 5], k = 2 → [ 4 , 5 , 1 , 2 , 3 ] k=2→[4,5,1,2,3] 。 
約束：面試官通常會要求你達成 O(1) 額外空間（原地修改）。 

二、 解題思路：三種不同層次的解法 在面試時，建議按照以下順序向面試官說明你的思考路徑： 
1. 輔助陣列法 (直覺解) — O(n) 空間 思路：建立一個新的陣列，利用公式計算每個數字旋轉後「新家」的地址。 
關鍵公式：new_index = (old_index + k) % n（ n 為陣列長度）。 
優點：邏輯最簡單，不容易出錯。 
缺點：面試官會追問：「能不能不使用額外空間？」 

2. 三次反轉法 (面試官最愛) — O(1) 空間 ★★★ 這是一個非常巧妙的數學觀察。
我們不需要一個一個搬移，而是透過三次反轉來達成「位移」的效果。 
以 nums = [1,2,3,4,5,6,7], k=3 為例： 
全反轉：將整個陣列反轉 →  [7,6,5,4,3,2,1]。 
反轉前 k 個：將前 3 個數字反轉 → [5,6,7,4,3,2,1]。 
反轉剩下 n−k 個：將後 4 個數字反轉 → [5,6,7,1,2,3,4]。 
結果：成功達成右旋轉 3 次！ 

3. 環狀替換法 (數學解) — O(1) 空間 
思路：把一個數字搬到它的目的地，再把目的地被趕出來的數字搬到下一個目的地，直到形成一個循環。 
難點：如果 n 和 k 有公因數，會陷入小循環，需要額外處理（利用 GCD 或計數器）。 

三、 實作技巧與通用模板 (面試必備) 
1. 處理 k 很大的情況 (非常重要) 面試官一定會問：「如果 k 是 100 萬，但陣列長度只有 10 怎麼辦？」 
技巧：k = k % n;。 這行程式碼必須寫在函式的最開頭。如果旋轉次數剛好是長度的倍數，等於沒轉。這能防止索引越界 (Out of Bounds)。 

2. 如何手寫 reverse (原地反轉) 如果你選擇「三次反轉法」，面試官可能會要求你不准用內建函式。 
通用的雙指標技巧： 設定 left 指向區間起點，right 指向終點。 while (left < right) 時，交換兩者內容。 left++, right--。 

3. 模運算 (%) 的通用性 在所有關於「循環」、「轉圈」或「位移」的題目中（例如：圓桌問題、棋盤移動），模運算 (%) 是最核心的工具。 
index + k 可能會爆掉。 (index + k) % n 則能確保指標永遠落在合法範圍內。

四、 複雜度分析 
時間複雜度： O(n) 。三次反轉法雖然做了三次反轉，但總操作次數大約是 2n ，在 Big O 表示法中仍是 O(n) 。 
空間複雜度：使用反轉法時為 O(1) ，這是面試最優解。 

五、 思考練習 當你掌握了「右旋轉」後，試著想一下： 如果題目改成 「左旋轉 (Left Rotation)」，反轉法要怎麼調整順序？ (提示：先反轉前 k 個，再反轉剩下，最後全反轉。)
*/

class Solution {
public:
    void rotate(vector<int>& nums, int k) {
        int n=nums.size();
        vector<int> res(n);

        k=k%n;
        if(k==0) return;

        for(int i=0; i<n; i++){
            res[(i+k)%n]=nums[i];
        }

        nums=res;
    }
};
