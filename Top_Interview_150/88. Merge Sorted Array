/*
一、 題目重點
輸入：兩個已排序的陣列 nums1 和 nums2。
空間限制：nums1 的長度被預先擴充到了 m + n，後面的 n 個空間暫時填 0。
目標：將 nums2 合併到 nums1 中，使合併後的 nums1 依然是排序好的。
核心要求：必須是 「原地 (In-place)」 修改，不能使用額外的輔助陣列。
二、 解題思路：逆向雙指標 (Two Pointers from Back)
這題最直覺的想法是從頭開始比，但你會發現：如果從頭開始比並放入 nums1，會覆蓋掉 nums1 還沒比過的數字，這樣就必須開一個新的陣列來存。
為了達成 O(1)
 空間複雜度，我們利用 nums1 後方的空位，採取「由後往前」的策略：
設定三個指標：
p1：指向 nums1 有效數字的末尾（索引 m - 1）。
p2：指向 nums2 的末尾（索引 n - 1）。
p_tail：指向 nums1 整個空間的最後面（索引 m + n - 1）。
比較與填空：
比較 nums1[p1] 和 nums2[p2]。
把較大的那個數字放到 nums1[p_tail] 的位置。
移動被選中的指標（p1 或 p2）以及 p_tail，繼續往左走。
處理剩下的數字：
當其中一個指標走完後，要檢查另一個指標。
如果是 p2 剩下了：代表 nums2 還有一些更小的數字沒放進去，直接把它們補到 nums1 的最前面。
如果是 p1 剩下了：代表 nums1 原本的數字本來就在對的位置，不需要動。
三、 實作技巧
1. 為什麼「由後往前」不會覆蓋？
因為 nums1 後方剛好有 n 個空位，這 n 個位置足以容納 nums2 的所有數字。當你把最大的數字往後放時，p_tail 永遠跑在 p1 的後方，絕對不會發生「還沒讀取的數字被覆蓋」的情況。
2. 邊界條件的處理
在寫 while 迴圈時，最穩健的寫法是：
while (p1 >= 0 && p2 >= 0)：這是兩邊都還有數字可以比的時候。
結束後，單獨處理 while (p2 >= 0)：這一步非常重要，因為如果 nums2 還有剩，必須全部補進 nums1。
3. 程式碼簡潔技巧
在更新時可以使用後置減減（如 nums1[p_tail--] = nums1[p1--]），這在 C++ 中非常常見，代表「賦值後，指標各自往左退一格」。
四、 複雜度分析
時間複雜度：O(m+n)
我們只需要遍歷一次兩個陣列的所有元素。
空間複雜度：O(1)
我們沒有使用任何額外的資料結構（如 vector 或 map），完全在原陣列上操作。
五、 面試官常見的追問
Q: 如果題目沒有說 nums1 後面有空位，怎麼辦？
A: 那就必須開一個 O(m) 或 O(m+n) 的輔助空間來存，或者接受 O((m+n)log(m+n)) 的排序代價。
Q: 為什麼不用 std::sort 直接合併再排序？
A: 雖然寫法最快，但排序的時間複雜度是 O((m+n)log(m+n))，不如雙指標的 O(m+n) 高效。面試時要先給出 O(m+n) 的解法。
*/

class Solution {
public:
    void merge(vector<int>& nums1, int m, vector<int>& nums2, int n){
        int index1=m-1, index2=n-1, index=m+n-1;

        while(index2>=0){
            if(index1>=0 && nums1[index1]>nums2[index2]){
                nums1[index]=nums1[index1];
                index1--;
            }else{
                nums1[index]=nums2[index2];
                index2--;
            }
            index--;
        }
    }
};


class Solution {
public:
    void merge(vector<int>& nums1, int m, vector<int>& nums2, int n){
        int index1=m-1, index2=n-1, index=m+n-1;

        for(int i=0;i<m+n;i++){
            if(index2<0) break;

            if(index1>=0 && nums1[index1]>nums2[index2]){
                nums1[index]=nums1[index1];
                index1--;
            }else{
                nums1[index]=nums2[index2];
                index2--;
            }

            index--;
        } 
    }
};




