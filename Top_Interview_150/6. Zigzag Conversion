這題可以用模擬法解。

我會準備 numRows 個字串，代表 Zigzag 的每一列。
接著從左到右掃過原字串，把每個字元放到目前的 row。

我用 curRow 紀錄目前所在列，用 direction 紀錄目前是往下還是往上。
當 curRow 到最上面或最下面時，就把方向反轉。

最後把所有 row 從上到下接起來，就是答案。

class Solution {
public:
    string convert(string s, int numRows) {
        if(numRows==1) return s;

        string ans="";
        vector<string> rows(numRows);
        int curRow=0, direction=0;

        for(char c : s){
            if(curRow==0)  direction=1;
            else if(curRow==numRows-1)  direction=-1;
            
            rows[curRow] += c;
            curRow += direction;
        }

        for(int i=0; i<numRows; i++){
            ans+=rows[i];
        }

        return ans; 
    }
};
