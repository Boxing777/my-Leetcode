/*
解題思路：貪婪演算法 —— 左右兩趟掃描法 (Two-Pass Greedy)
面試中遇到這種「同時被左右兩邊限制」的問題，最神級的破解法就是：「把條件拆開看（解耦），先滿足左邊，再滿足右邊」。
Step 1: 確保底線 (初始化)
既然每個人至少要有 1 顆糖果，那我們就先準備一個跟小朋友人數一樣多的 candies 陣列，全部填入 1。
Step 2: 第一趟掃描（由左至右）—— 只管左邊的鄰居
我們從左走到右，只看左邊的鄰居。
規則：如果我（i）的評分比左邊（i-1）高，那我的糖果就要比左邊多 1 顆。
動作：candies[i] = candies[i-1] + 1。
這趟走完後，所有「右邊比左邊高」的規則都已經完美達成了。
Step 3: 第二趟掃描（由右至左）—— 只管右邊的鄰居
接著，我們反過來，從右走到左，只看右邊的鄰居。
規則：如果我（i）的評分比右邊（i+1）高，那我的糖果必須比右邊多 1 顆。
動作：這裡有個致命陷阱（面試考點）！
如果我原本的糖果（在第一趟拿到的）已經比右邊多 1 顆了，我還需要改嗎？不需要！
為了同時滿足第一趟（左邊）跟第二趟（右邊）的條件，我的糖果數必須是 「原本的糖果」與「右邊糖果 + 1」這兩者之間的『最大值』。
所以邏輯是：candies[i] = max(candies[i], candies[i+1] + 1)。
Step 4: 加總
兩趟掃描結束後，每個人都完美滿足了左右鄰居的限制。我們只需要把 candies 陣列裡所有的數字加起來，就是最少需要的總糖果數。

*/


class Solution {
public:
    int candy(vector<int>& ratings) {
        int n=ratings.size();
        int ans=0;
        vector<int> candy(n,1);

        for(int i=1;i<n;i++){
            if(ratings[i]>ratings[i-1]){
                candy[i]=candy[i-1]+1;
            }
        }

        for(int i=n-2;i>=0;i--){
            if(ratings[i]>ratings[i+1]){
                candy[i]=max(candy[i],candy[i+1]+1);
            }
        }

        for(int i=0;i<n;i++){
            ans+=candy[i];
        }

        return ans;
    }
};
