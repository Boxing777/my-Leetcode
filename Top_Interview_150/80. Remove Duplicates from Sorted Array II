/*
一、 題目講解
輸入：一個已經由小到大排序好的整數陣列 nums。
規則：
每個元素「最多隻能出現兩次」。
如果是第三次出現，就必須移除。
目標：在原地修改陣列，回傳修改後的長度 k，且 nums 的前 k 個元素必須是符合規則的結果。
限制：必須使用 O(1) 的額外空間（不能開新陣列）。
二、 解題思路：通用快慢指標法 (Slow-Fast Pointers)
這題最好的解法不是去「刪除」元素（因為陣列刪除是 O(n)），而是用「覆蓋」的方式。我們可以使用兩個指標來完成：
慢指標 (slow)：代表「下一個有效的數字應該存放的位置」。它同時也代表了目前已經處理好的、符合規則的子陣列長度。
快指標 (fast)：用來遍歷整個原始陣列，檢查每一個數字是否符合「放入」的資格。
核心邏輯：如何判斷 fast 指向的數字能不能放？
因為陣列是排序好的，所以重複的數字一定會連在一起。
如果要允許最多出現 K 次（這題 K=2），我們只需要把 fast 指向的數字，跟已經放好的倒數第 K 個數字比較。
如果 nums[fast] 不等於 nums[slow - 2]，代表這個數字還沒出現超過兩次，可以放進 nums[slow]。
如果等於，代表 nums[slow-1] 和 nums[slow-2] 肯定都已經是這個數字了，所以 fast 指向的這個數字就是第三個，我們必須跳過它。
三、 實作技巧（通用模板）
這類題目有一個「萬用模板」，無論題目要求最多重複 1 次、2 次還是 K 次都能解。
1. 處理邊界
如果陣列長度小於等於 2，它絕對不會違反規則，直接回傳長度即可。
2. 初始化指標
從索引 2 開始處理，因為前兩個數字不論是否重複都是合法的。
slow = 2
fast 從 2 遍歷到最後。
3. 比較與移動
if (nums[fast] != nums[slow - 2])
nums[slow] = nums[fast]
slow++
最後回傳 slow。
四、 面試加分技巧：為什麼這個邏輯很專業？
通用性 (Generalization)：
面試官很喜歡問：「如果現在改成最多重複 3 次怎麼辦？」
如果你是用這套邏輯，你只需要把代碼中的 2 改成 3 即可。這種具備擴充性的邏輯（Scalable Logic）會讓面試官覺得你的設計思考很全面。
原地操作 (In-place)：
不需要使用 erase 函式。在陣列中使用 erase 會導致後方元素大量平移，時間複雜度會變得很差。利用「覆蓋」的思想是 O(n)
 時間最優解。
短路邏輯與簡潔性：
不需要額外的計數器（Counter）來數這個數字出現幾次了。利用「有序陣列」的特性，直接比對 slow - K 的位置，是最精簡且不易出錯的寫法。
*/

class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if(nums.size()<2) return nums.size();

        int slowIndex=2,fastIndex=2;

        for(fastIndex; fastIndex<nums.size(); fastIndex++){
            if(nums[fastIndex]!=nums[slowIndex-2]){
                nums[slowIndex]=nums[fastIndex];
                slowIndex++;
            }
        }


        return slowIndex--;
    }
};
