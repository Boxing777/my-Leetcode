/*
一、 題目白話講解 
基本規則：羅馬數字由 7 個符號組成（I=1, V=5, X=10, L=50, C=100, D=500, M=1000）。
通常情況下，羅馬數字是從左到右、從大到小排列的，你只要把它們對應的數值全部加起來就好（例如：VI = 5 + 1 = 6）。 
例外規則（陷阱）：有 6 種特殊情況，小的數字會出現在大的數字「左邊」，這時代表減法。 I 在 V 或 X 左邊：代表 4 (IV) 或 9 (IX)。 
X 在 L 或 C 左邊：代表 40 (XL) 或 90 (XC)。 C 在 D 或 M 左邊：代表 400 (CD) 或 900 (CM)。 
目標：給你一個正確的羅馬數字字串，請你把它轉換成阿拉伯數字（整數）。 

二、 核心解題思路：
尋找「通用規律」 面試中最忌諱的寫法，就是用一堆 if-else 去把 IV, IX, XL 等 6 種特例「寫死（Hardcode）」。
面試官期待看到的是你能找出隱藏在背後的數學通用規律。 
我們來觀察一下： VI (6) → V (5) 在前，I (1) 在後。因為 5≥1 ，所以是相加： 5+1=6 。 IV (4) → I (1) 在前，V (5) 在後。因為 1<5 ，所以 I 是用來相減的： −1+5=4 。 
🌟 破局的通用規律： 當我們由左至右讀取字串時，只要比較**「當前字母代表的數字」與「下一個字母代表的數字」**： 如果 當前數字 < 下一個數字 → 這個數字是要被減去的。 
如果 當前數字 ≥ 下一個數字 → 這個數字是要被加上的。 最後一個數字沒有「下一個」可以比較，它永遠是被加上的。 有了這個規律，管它是 IV 還是 CM，我們都不需要特判，一套邏輯通殺！
*/


class Solution {
public:
    int romanToInt(string s) {
        unordered_map<char,int> roman={
            {'I', 1}, {'V', 5}, {'X', 10}, {'L', 50},
            {'C', 100}, {'D', 500}, {'M', 1000}
        };

        int sum=0;
        int n=s.size();

        for(int i=0;i<n-1;i++){
            if(roman[s[i]]<roman[s[i+1]]){
                sum-=roman[s[i]];
            }else{
                sum+=roman[s[i]];
            }
        }

        sum+=roman[s[n-1]];
        return sum;
    }
};
