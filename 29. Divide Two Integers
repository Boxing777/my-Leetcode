/*
一、 題目重點
限制條件：不能使用乘法 (*)、除法 (/) 或取餘數 (%)。
整數溢位：環境只支援 32 位元有符號整數。範圍是[-2^31 ~ 2^31-1] 最著名的溢位案例：
−2147483648÷−1=2147483648，這超出了 INT_MAX，必須回傳 INT_MAX。
目標：計算「商」，且向零取整。
二、 解題思路：二進位長除法 (Exponential Search)
如果你用「減法」來模擬除法（例如 10÷3 就是看 10 可以減幾次 3），效率會太慢。如果被除數是 20 億，除數是 1，會跑 
20 億次，導致 TLE (Time Limit Exceeded)。我們需要像「長除法」一樣，每次不是減去 
1 倍，而是減去 2^n 倍，這就是利用 「左移位運算 (<<)」。

1. 處理符號與絕對值
先判斷結果是正還是負。技巧：bool isNegative = (dividend > 0) != (divisor > 0);。
將 dividend 和 divisor 都轉為正數。
注意：在 C++ 中 abs(INT_MIN) 會溢位。建議在運算時先轉成 long long，或者統一轉成負數運算（因為負數的範圍比正數大 1 ）。
2. 指數級倍增減法 (核心邏輯)
假設我們要算 
60÷8：
8×1=8
8×2=16
8×4=32
8×8=64（太大了，回頭看上一個）

三、 實作技巧與避坑指南
1. 避免溢位的「負數法」
如果你在面試中不被允許使用 long long，一個進階技巧是將所有數字轉成 「負數」 來計算。
原因：int 的範圍是 -2147483648 到 2147483647。
正數 2147483648 存不下，但負數 -2147483648 存得下。
2. 位元移位的安全檢查
在執行 divisor << (k + 1) 之前，要確保這個動作不會讓數字爆掉。
在 C++ 中，對帶正負號的整數進行左移如果造成溢位，是未定義行為。
技巧：檢查 (temp_divisor << 1) <= temp_dividend 之前，先確保 temp_divisor 不會超過最大值的一半。
*/

class Solution {
public:
    int divide(int dividend, int divisor) {
        if(dividend==0) return 0;
        if(dividend==INT_MIN && divisor==-1) return INT_MAX;
        if(dividend==INT_MIN && divisor==1) return INT_MIN;

        bool isPositive= (dividend>0)==(divisor>0);
        int ans=0;

        if(dividend>0) dividend=-dividend;
        if(divisor>0) divisor=-divisor;

        while(divisor>=dividend){
            int tempDivisor=divisor;
            int tempAns=1;

            while(tempDivisor>=-1073741824 && (tempDivisor<<1)>=dividend){
                tempDivisor=tempDivisor<<1;
                tempAns=tempAns<<1;
            }

            dividend-=tempDivisor;
            ans+=tempAns;
        }
        return isPositive? ans: -ans;
    }
};
